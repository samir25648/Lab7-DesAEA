<Window x:Class="Lab.InvoicesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Listado de Facturas" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <TextBlock Name="timeTextBlock" HorizontalAlignment="Center" FontSize="16"/>
        </StackPanel>
        <ListView Name="invoiceListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding invoice_id}" />
                    <GridViewColumn Header="ID Cliente" Width="100" DisplayMemberBinding="{Binding customer_id}" />
                    <GridViewColumn Header="Fecha" Width="200" DisplayMemberBinding="{Binding date}" />
                    <GridViewColumn Header="Total" Width="100" DisplayMemberBinding="{Binding total}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
